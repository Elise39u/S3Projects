name: Java pipeline with Maven
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check out repository code
        run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event. Testie?"